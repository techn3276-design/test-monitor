# Test Monitor

> Простой мониторинг процесса `test` в Linux

## Описание

Этот проект содержит минимальный набор файлов для мониторинга процесса `test`. Скрипт проверяет состояние процесса и каждые 60 секунд отправляет запрос на мониторинг-сервер.

---

## 🔧 Как работает мониторинг

- **Автозапуск**: Запускается при старте системы через systemd
- **Периодичность**: Выполняется каждую минуту
- **Логика работы**:
  - **Если процесс `test` запущен**:
    - Проверяет, был ли перезапуск процесса
    - При обнаружении перезапуска записывает событие в `/var/log/monitoring.log`
    - Отправляет HTTPS-запрос на: `https://test.com/monitoring/test/api`
    - При недоступности сервера записывает ошибку в лог
  - **Если процесс не запущен**: Скрипт ничего не делает

---

## 📁 Структура репозитория

```
.
├── test_monitor.sh            # Bash-скрипт мониторинга
├── test-monitor.service       # systemd-сервис
├── test-monitor.timer         # systemd-таймер (каждую минуту)
└── README.md                  # Документация (этот файл)
```

---

## 🚀 Установка и настройка

### 1. Подключение к удалённому серверу

На локальном компьютере выполните:

```bash
ssh USER@SERVER_IP
```

**Пример**:
```bash
ssh root@192.168.1.10
```

> После подключения все команды выполняются на сервере.

### 2. Клонирование репозитория

```bash
git clone https://github.com/techn3276-design/test-monitor.git
cd test-monitor
```

Проверьте наличие файлов:

```bash
ls -1
```

**Ожидаемый вывод**:
```
README.md
test-monitor.service
test-monitor.timer
test_monitor.sh
```

### 3. Установка мониторинга

#### 3.1 Установка скрипта

```bash
sudo install -m 0755 test_monitor.sh /usr/local/bin/test_monitor.sh
```

#### 3.2 Установка systemd-юнитов

```bash
sudo install -m 0644 test-monitor.service /etc/systemd/system/test-monitor.service
sudo install -m 0644 test-monitor.timer   /etc/systemd/system/test-monitor.timer
```

#### 3.3 Активация и запуск таймера

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now test-monitor.timer
```

✅ **Мониторинг теперь работает и выполняется каждую минуту!**

---

## 🧪 Проверка работы

### Проверить статус таймера

```bash
systemctl status test-monitor.timer
```

**Ожидаемый результат**:
```
Active: active (waiting)
```

### Проверить последние выполнения сервиса

```bash
sudo journalctl -u test-monitor.service --since "10 min ago"
```

### Создать тестовый процесс

Если процесс `test` еще не запущен, можно создать его для проверки:

```bash
echo -e '#!/bin/bash\nwhile true; do sleep 10; done' | sudo tee /usr/local/bin/test >/dev/null
sudo chmod +x /usr/local/bin/test
/usr/local/bin/test &
disown
```

**Проверка**:

```bash
pgrep -x test
```

---


## 📄 Просмотр логов мониторинга

Лог записывается в: `/var/log/monitoring.log`

### Просмотр логов в реальном времени

```bash
tail -f /var/log/monitoring.log
```
